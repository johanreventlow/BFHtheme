# Workflow derived from https://github.com/r-lib/actions/tree/v2/examples
# Lint runs quickly and catches style regressions before package checks finish.
name: lint

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

permissions: read-all

jobs:
  lint:
    runs-on: ubuntu-latest
    # BFHtheme has pre-existing lint debt. Keep this visible while avoiding
    # blocking the initial develop/main workflow rollout.
    continue-on-error: true
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - uses: actions/checkout@v4

      - uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      - name: Install lintr
        run: install.packages("lintr", repos = "https://packagemanager.posit.co/cran/latest")
        shell: Rscript {0}

      - name: Lint
        run: lintr::lint_package()
        shell: Rscript {0}
